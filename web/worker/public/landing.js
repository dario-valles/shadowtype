/* ============================================================================
   Shadowtype landing — no-op.
   Shadowtype is free and open source: there is no pricing, tranche, or sticky
   purchase bar, so this script intentionally does nothing. It is kept only so
   pages that still reference /landing.js don't 404.
   ========================================================================== */
